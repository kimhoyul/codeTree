#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int y, m, d;
    cin >> m;
    cin.get();
    cin >> d;
    cin.get();
    cin >> y;
    printf("%d.%d.%d",y, m, d);
    return 0;
}